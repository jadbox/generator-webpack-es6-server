'use strict'

const assert = require('assert');

//const project = require('../dist/<%= projectName %>.js');

describe('## Test for <%= projectName %>', () => {
  describe('# Test 1', () => {
    it('completes', function(done) {
      assert.ok(true === true);
      done();
    });

  });
});