{
  "name": "<%= projectName %>",
  "version": "0.0.1",
  "description": "<%= projectDescription %>",
  "scripts": {
    "start": "node dist/<%= projectName %>.js",
    "build": "webpack --config webpack.config.js  --progress --colors --watch",
    "test": "mocha --harmony -R spec -t 5000 test/*.js"
  },
  "main": "dist/<%= projectName %>.js",
  "author": "<%= authorName %> <<%= authorEmail %>>",
  "license": "<%= license %>",
  "dependencies": {
  },
  "devDependencies": {
    "webpack": "^1.12.14",
    "babel-core": "^6.7.0",
    "babel-loader": "^6.2.4",
    "babel-plugin-transform-function-bind": "^6.5.2",
    "babel-preset-es2015": "^6.6.0",
    "path": "^0.12.7",
    "mocha": "^2.4.5"
  }
}
